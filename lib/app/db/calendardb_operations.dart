import 'package:recipe_app/app/ui/components/calendar.dart';
import 'package:sqflite/sqflite.dart';
import 'database.dart';

// should map into calendar_day Model, similar to grocerydb_operations so that we can use Calendar object for displaying


class CalendarOperations {
  final DatabaseHelper _dbHelper = DatabaseHelper();

  // query through Meal tables to find Calendar.breakfast, etc? idk
}


